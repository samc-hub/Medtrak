# Local development uses SQLite (zero config).
DATABASE_URL="file:./dev.db"

# Secret used to sign session JWTs. Generate one with: openssl rand -base64 32
AUTH_SECRET="replace-me-with-a-long-random-string"
